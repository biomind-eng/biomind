# Icons

Drop SVG / PNG icons here. The current site uses emoji glyphs for pillar/thrust icons, but bespoke SVG icons can replace them for a more polished look once available.

Reference from a `.qmd` page using standard Markdown image syntax or HTML.
